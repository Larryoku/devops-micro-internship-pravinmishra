# Terraform Blueprint Spec
Generate a basic AWS or provider-agnostic baseline containing:
- `providers.tf` (Setting up local or cloud providers)
- `main.tf` (Core resources)
- `variables.tf` (Input variables)
- `outputs.tf` (Exposed deployment outputs)