---
name: scaffold-terraform
description: Initializes a comprehensive, production-ready Terraform structure.
allowed-tools: [Bash, Write, Read]
---

# Scaffold Terraform
When the user invokes `/scaffold-terraform`, use the configuration found in `template-spec.md` to generate a structured, multi-file Terraform setup inside a `/terraform` root directory. Ensure best practices like separate variables, outputs, and provider definitions.